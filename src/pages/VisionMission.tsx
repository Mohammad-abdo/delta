import VisionMissionSection from "@/components/VisionMissionSection";

/**
 * Vision & Mission Page
 * 
 * Dedicated page displaying the company's vision and mission statements.
 */
const VisionMission = () => {
  return (
    <div className="py-8">
      <VisionMissionSection />
    </div>
  );
};

export default VisionMission;

